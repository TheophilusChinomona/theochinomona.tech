-- Rollback: Cannot remove enum values once added in PostgreSQL
-- Note: PostgreSQL does not support removing enum values
-- This migration cannot be fully rolled back

-- The enum values will remain in the database
-- If needed, a new enum type would need to be created and the column migrated

